{
  "name": "sobus",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app",
    "docusaurus"
  ],
  "scripts": {
    "prepare": "node -e \"try { require('husky') } catch (e) { process.exit(0) }\""
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
